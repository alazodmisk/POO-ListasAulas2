==QUESTÕES A SEREM RESPONDIDAS POR ETAPA==
(1) Quais classes aparecem no problema?
(2) Quais atributos são necessários?
(3) Qual pacote deve receber cada classe?
(4) Quais endpoints serão necessários?
(5) Existe alguma regra de negócio? 
(6) Essa regra deve ficar no controller, no service ou no model?
(7) Existe algum relacionamento entre classes?
(8) Existe algum estado que pode virar enum?


==ATIVIDADE 1==
(1) As classes Produto e Orcamento representam as entidades
(2) As classes que recebem requisições HTTP são as classes ProdutoController e OrcamentoController
(3) As regras de negócio são executadas pelas classes ProdutoService e OrcamentoService
(4) As interfaces repository são as responsáveis por conversar com o banco de dados
(5) Com requisição do tipo Post http://localhost:8090/orcamento visto que não há nada escrito entre parêntes na anotação do @PostMapping
(6) Com requisição do tipo Get http://localhost:8090/produtos visto que não há nada escrito entre parêntese no @GetMapping Entretanto, se buscamos por apenas um produto temos http://localhost:8090/produtos/{id}

==ATIVIDADE 2==
(1) ProdutoService, ProdutoController, ProdutoRepository e Produto
(2) Os atributos utilizados foram os atributos já presentes na entidade produto
(3) As classes já estavam alocadas em seus devidos pacotes
(4) Um novo endpoint foi criado, para a requisão PUT com o objetivo de se alterar o preço do produto depois de salvo no banco de dados
(5) As regras de negócio padrão como deletar ou achar por id e a nova regra de negócio do tipo PUT para atualizar o preco de um produto
(6) A regra está no Service com acesso via controller
(7) Não
(8) Não

==ATIVIDADE 3==
(1) Cliente[entidade]; ClienteController[endpoints]; ClienteService[Regras De Negócio]; ClienteRepository[Permanencia no Banco de Dados]
(2) Atributos de ID para cliente, nome, email e telefone;
(3) Cliente[@Entity]; ClienteController[@RestController e @RequestMapping]; ClienteRepository[@repository]; ClienteService[@Service]
(4) http://localhost:8090/cliente e http://localhost:8090/cliente/{id}, variando o tipo de requisição
(5) FindAll, FindById, DeleteById, save
(6) As regras de negócio ficam no service, o controller apenas acessam elas
(7) Não há relacionamento de heranças entre as classes, apenas se comunicam entre si com exceção do repository que recebe extends de JPA
(8) Cliente pode receber um atributo do tipo Status em que diz se foi aprovado seu cadastro, recusado ou se analise esta pendente (PENDENTE, RECUSADO OU APROVADO)

==ATIVIDADE 4==
(1) ClienteService
(2) email e nome
(5) Regra de negócio adicionada -> Email e nome do cliente não pode ser vazio
(6) Regra no service

==ATIVIDADE 5==
(1) ClienteService, ClienteController e ClienteRepository
(2) Atributos trabalhados: email
(4) localhost:8090/email/{email}
(5) findByEmail
(6) Regra no service porém com acesso via controller

==ATIVIDADE 6, 7 e 8==
(1) Todas de orcamento exceto repository
(2) statusOrcamento
(4) localhost:8090/{id}/aprovar e localhost:8090/{id}/rejeitar
(5) O comportamento de aprovar ou rejeitar o orcamento foi movida para o model de orcamento, desse modo o service chama esses métodos de acordo com o pedido do controller
(8) Foi utilizado o ENUM StatusOrcamento

obs após realizar todas as atividades até a 13
Ao mudar o status de um orçamento de pendente para autorizado ou rejeitado, o mesmo muda no h2-console provando que sim, o status está sendo alterado pela requisição porém ao utilizar o método get o json ainda vem como pendente.